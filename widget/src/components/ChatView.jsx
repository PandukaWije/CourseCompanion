import React from 'react';
import { ChatPageLayout } from './chat/ChatPageLayout';

/**
 * ChatView Component
 * Main view for the chat interface
 * Uses the new two-panel layout with chat on left and tools on right
 */
const ChatView = () => {
  return <ChatPageLayout />;
};

export default ChatView;